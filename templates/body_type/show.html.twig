{% extends 'base.html.twig' %}

{% block title %}BodyType{% endblock %}

{% block heading_h1 %}
    Type de corps: {{ body_type.name }}
{% endblock %}
{% block heading_btn %}

    <div class="btn-group" role="group">
        <a href="{{ path('body_type_index') }}"
           class="btn btn-sm btn-primary shadow-sm"><i
                    class="fas fa-arrow-left fa-sm text-white-50"></i> retour à la liste
        </a>
        <a href="{{ path('body_type_edit', {'id': body_type.id}) }}"
           class="btn btn-sm btn-info shadow-sm"><i
                    class="fas fa-edit fa-sm text-white-50"></i> Modifier
        </a>

    </div>

{% endblock %}

{% block content %}
    <div class="card shadow mb-4">
        <div class="card-body">

            <table class="table">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ body_type.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ body_type.name }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ body_type.description }}</td>
                </tr>
                <tr>
                    <th>Logo</th>
                    <td>{{ body_type.logo }}</td>
                </tr>
                </tbody>
            </table>

            <hr/>

            {{ include('body_type/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
