{% extends 'base.html.twig' %}

{% block title %}Edit ContractOption{% endblock %}

{% block heading_h1 %}
{{ 'Edit'|trans }}  ContractOption{% endblock %}

{% block heading_btn %}

<div class="btn-group" role="group">
    <a href="{{ path('contract_option_index') }}" class="btn btn-sm btn-primary shadow-sm">
        <i class="fas fa-arrow-left fa-sm text-white-50"></i>
        {{ 'back to list'|trans }}
    </a>
</div>

{% endblock %}
{% block content %}
<div class="card shadow mb-4">
    <div class="card-body">
        {{ include('contract_option/_form.html.twig', {'button_label': 'Update'}) }}
        <hr/>
        {{ include('contract_option/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
