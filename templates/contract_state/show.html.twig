{% extends 'base.html.twig' %}

{% block title %}ContractState{% endblock %}

{% block heading_h1 %}
    ContractState{% endblock %}

{% block heading_btn %}

    <div class="btn-group" role="group">

        <a href="{{ path('contract_state_index') }}" class="btn btn-sm btn-primary shadow-sm">
            <i class="fas fa-arrow-left fa-sm text-white-50"></i> {{ 'back to list'|trans }} </a>

        <a href="{{ path('contract_state_edit', {'id': contract_state.id}) }}"
           class="btn btn-sm btn-info shadow-sm">
            <i class="fas fa-edit fa-sm text-white-50"></i> {{ 'edit'|trans }}
        </a>

    </div>

{% endblock %}


{% block content %}
    <div class="card shadow mb-4">
        <div class="card-body">
            <table class="table">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ contract_state.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ contract_state.name }}</td>
                </tr>
                <tr>
                    <th>Color</th>
                    <td>{{ contract_state.color }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ contract_state.description }}</td>
                </tr>
                <tr>
                    <th>Active</th>
                    <td>{{ contract_state.active ? 'Yes' : 'No' }}</td>
                </tr>
                </tbody>
            </table>
            <hr/>
            {{ include('contract_state/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

